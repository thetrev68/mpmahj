You are about to start Phase 4 (Event Bridge Integration) of the GameBoard.tsx refactoring plan. This phase focuses on replacing the monolithic inline event handling in GameBoard.tsx with pure function event handlers.

**Phase 4 Objectives:**
- Replace inline event handling with pure function calls
- Create an event bridge hook (useGameEvents.ts)
- Connect WebSocket to pure event handlers
- Dispatch state updates and side effects
- Create the SetupPhase component
- Add feature flag for gradual rollout

**Key Files to Modify/Create:**
1. `apps/client/src/hooks/useGameEvents.ts` - Event bridge hook
2. `apps/client/src/components/game/phases/SetupPhase.tsx` - Setup phase component
3. `apps/client/src/components/game/GameBoard.tsx` - Integrate event bridge
4. `apps/client/src/lib/game-events/types.ts` - Add any missing types

**Tasks to Complete:**
1. Implement `useGameEvents.ts` - connect to WebSocket, call pure event handlers, dispatch actions
2. Write tests for useGameEvents.ts
3. Create SetupPhase.tsx with tests
4. Add feature flag `USE_EVENT_BRIDGE`
5. Integrate event bridge into GameBoard.tsx
6. Run all integration tests to ensure they pass

**Existing Resources:**
- Pure event handlers already implemented in `lib/game-events/`
- Side effect manager in `lib/game-events/sideEffectManager.ts`
- Type definitions in `lib/game-events/types.ts`
- Use the existing `useGameSocket.ts` hook for WebSocket management

Follow the architecture and patterns established in Phases 1-3. Ensure all integration tests pass before completing the phase.