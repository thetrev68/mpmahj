// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { CharlestonStage } from "../../../../../../../apps/client/src/types/bindings/generated/CharlestonStage";
import type { GameResult } from "./GameResult";
import type { SetupStage } from "./SetupStage";
import type { TurnStage } from "./TurnStage";
import type { WinContext } from "./WinContext";

/**
 * The top-level game phase that governs what type of activity is currently happening.
 *
 * This is the root of the hierarchical state machine. Each phase may contain
 * sub-phases that track more granular states (e.g., [`CharlestonStage`], [`playing::TurnStage`]).
 *
 * # State Transitions
 *
 * ```text
 * WaitingForPlayers
 *   ↓ AllPlayersJoined
 * Setup(RollingDice → BreakingWall → Dealing → OrganizingHands)
 *   ↓ HandsOrganized
 * Charleston(FirstRight → ... → Complete)
 *   ↓ CharlestonComplete
 * Playing(Drawing ↔ Discarding ↔ CallWindow)
 *   ↓ MahjongDeclared
 * Scoring
 *   ↓ ValidationComplete
 * GameOver
 * ```
 *
 * # Examples
 *
 * ```
 * use mahjong_core::flow::{GamePhase, PhaseTrigger, SetupStage};
 *
 * let phase = GamePhase::WaitingForPlayers;
 * let next = phase.transition(PhaseTrigger::AllPlayersJoined).unwrap();
 * assert_eq!(next, GamePhase::Setup(SetupStage::RollingDice));
 * ```
 */
export type GamePhase = "WaitingForPlayers" | { "Setup": SetupStage } | { "Charleston": CharlestonStage } | { "Playing": TurnStage } | { "Scoring": WinContext } | { "GameOver": GameResult };
